# Copilot Agent Operation Guide

Read this operation guide at each agent action

## On Updates

1. Make sure to run tests located at the `test` directory and ensure all tests pass.
2. Make sure changes are consistent in `action.yaml`, `.github/workflows`, `entrypoint.sh`, and `README.md`.
3. Make sure to update both description and examples in `README.md`.